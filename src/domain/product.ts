export class Product {
    name: string
    url: string
    supplierName: string

    constructor(name: string, url: string, supplierName: string) {
        this.name = name
        this.url = url
        this.supplierName = supplierName
    }
}